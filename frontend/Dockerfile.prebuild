# 预编译模式：本地已有的 forecast_claude-frontend（内含 nginx）
# 使用前先在本地执行: npm install && npm run build
FROM forecast_claude-frontend:latest

COPY dist/ /usr/share/nginx/html/
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 7000
